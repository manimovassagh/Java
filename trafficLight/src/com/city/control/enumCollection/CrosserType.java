package com.city.control.enumCollection;

public enum CrosserType {

	/**
	 * Define a CrossType as Passenger and Automobile because these values must not
	 * ever Change or mixied up!
	 */
	PASSENGER, AUTOMOBILE
}
